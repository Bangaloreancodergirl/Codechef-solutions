#include <stdio.h>
#include <math.h>
int main(void)
{
    int t;
    scanf("%d",&t);
    while(t--)
    {
       int k,x=0;
       scanf("%d",&k);
       while(k%2==0)
       {
           x++;
           k=k/2;
       }
       printf("%d\n",x);
    }
    return 0;
}
